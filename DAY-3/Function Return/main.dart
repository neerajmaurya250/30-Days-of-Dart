void main() {
  int result = Add(25, 36);
  print(result);
}

int Add(int num1, int num2) {
  // Any other operation done before return in this function will be executed only.
  return num1 + num2;
}
